spring:
    application:
        name: monthly_expense_backend
    datasource:
        password: bangtan
        url: jdbc:postgresql://localhost:5432/MonthlyExpense
        username: postgres
    jpa:
        show-sql: true
        hibernate:
            ddl-auto: update
    logging:
        level:
            root: ERROR
    cloud:
            compatibility-verifier:
                enabled: false

mail:
    url: http://localhost:8082/v1/mail